---
description: Remove the demo branch and return to real vault state on main
argument-hint: (no arguments needed)
allowed-tools: Bash(bash:*), Bash(git checkout:*), Bash(git stash:*), Bash(git branch:*)
---

Run the demo-cleanup script:

```bash
bash .claude/commands/demo-cleanup.sh
```

Output the result and stop.
